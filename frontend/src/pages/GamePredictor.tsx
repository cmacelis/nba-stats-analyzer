import React from 'react';
import { Typography } from '@mui/material';

const GamePredictor: React.FC = () => {
  return (
    <Typography variant="h4">
      Game Predictor (Coming Soon)
    </Typography>
  );
};

export default GamePredictor; 